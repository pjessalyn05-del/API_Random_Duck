# API_Random_Duck
A fun web app that fetches random duck images from a public API and displays them instantly.

random-duck-app/
│
├─ index.html       # Main HTML file
├─ index.css        # Styling
└─ index.js         # JavaScript logic (fetch API and display)

Setup Instructions:

Clone or download this repository:
git clone https://github.com/yourusername/random-duck-app.git
Open the app in a browser
Double-click index.html
Or use VS Code Live Server for auto-refresh
tart fetching ducks
Click the “Get Duck” button
Watch a random duck appear
Technologies Used
HTML5
CSS3 (Flexbox/Grid for layout)
JavaScript (Fetch API & DOM manipulation)
Public Random Duck API
